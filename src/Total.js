export default function Total({nums})
{
    // let add=0;
    // for(let i=0;i<nums.length;i++)
    // {
    //     add+=nums[i];
    // }

    return(
        <p>{nums}</p>
        //<p>total ={add}</p>
    )
}